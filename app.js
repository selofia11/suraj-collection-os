let d;const v=document.getElementById('view'),n=[...document.querySelectorAll('nav button')];function a(x){n.forEach(b=>b.classList.toggle('active',b.dataset.view===x))}fetch('cards.json').then(r=>r.json()).then(j=>{d=j;h()});function h(){a('home');v.innerHTML=`<div class=hero><h2>Mission Control</h2></div><div class=grid><div class=metric><span>Cards</span><b>${d.cards.length}</b></div><div class=metric><span>Players</span><b>${new Set(d.cards.map(c=>c.player)).size}</b></div></div>`+d.cards.slice(0,3).map(c=>`<div class=card><h3>${c.card}</h3><p>${c.player} · ${c.grade}</p></div>`).join('')}