let DB,view='home',player='';const app=document.getElementById('app');fetch('cards.json').then(r=>r.json()).then(d=>{DB=d;draw()});const P=['Lamine Yamal','Jude Bellingham','Cooper Flagg','Victor Wembanyama','Bo Nix','Shohei Ohtani'];window.setV=v=>{view=v;player='';draw()};window.openP=p=>{view='player';player=p;draw()};function nav(){return `<nav><button class='${view=='home'?'on':''}' onclick="setV('home')">Home</button><button class='${view=='vault'?'on':''}' onclick="setV('vault')">Vault</button><button class='${view=='analytics'?'on':''}' onclick="setV('analytics')">Analytics</button><button class='${view=='sniper'?'on':''}' onclick="setV('sniper')">Sniper</button></nav>`}function draw(){let h=`<h1>Card Vault</h1><div class=hero><h2>Mission Control</h2><div class=grid><div class=metric><div>Cards</div><b>${DB.cards.length}</b></div><div class=metric><div>Players</div><b>${new Set(DB.cards.map(c=>c.player)).size}</b></div></div></div>`;if(view=='home'||view=='vault'){P.forEach(p=>h+=`<div class=card onclick="openP('${p}')"><h2>${p}</h2><div>${DB.cards.filter(c=>c.player==p).length} cards</div></div>`)}if(view=='player'){h+=`<button onclick="setV('vault')">← Back</button><h2>${player}</h2>`;DB.cards.filter(c=>c.player==player).forEach(c=>h+=`<div class=card><h3>${c.title}</h3><div>${c.grade} · ${c.rarity}</div></div>`)}if(view=='analytics'){const by={};DB.cards.forEach(c=>by[c.player]=(by[c.player]||0)+1);h+='<div class=hero><h2>Analytics</h2></div>';Object.entries(by).forEach(([k,v])=>h+=`<div class=card><b>${k}</b><div>${v} cards</div></div>`)}if(view=='sniper'){h+='<div class=hero><h2>Sniper Board</h2><div>Coming next: live eBay targets.</div></div>'}app.innerHTML=h+nav()}